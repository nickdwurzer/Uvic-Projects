My code is contained in a Jupyter Notebook. Ensure necessary dependencies are installed in your environement before running(numpy, matplotlib and sklearn).
Also please ensure that the data file is in the same directory as the jupyter notebook, or change the file path in the notebook.
